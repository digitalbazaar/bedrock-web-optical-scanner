{
  "name": "bedrock-web-optical-scanner-test",
  "version": "0.0.1-0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --preserve-symlinks test.js test --framework karma",
    "test-karma": "node --preserve-symlinks test.js test --framework karma",
    "debug": "node --preserve-symlinks test.js test --framework karma --log-level debug",
    "coverage": "node --preserve-symlinks test.js test --framework karma-coverage",
    "coverage-ci": "node --preserve-symlinks test.js test --framework karma-coverage-ci"
  },
  "dependencies": {
    "@bedrock/core": "^6.0.1",
    "@bedrock/express": "^8.0.0",
    "@bedrock/karma": "^7.0.1",
    "@bedrock/test": "^8.0.5",
    "@bedrock/views": "^11.0.0",
    "@bedrock/web": "^3.0.0",
    "@bedrock/webpack": "^9.0.0",
    "@bedrock/web-optical-scanner": "file:..",
    "barcode-detector": "^3.0.5"
  }
}
